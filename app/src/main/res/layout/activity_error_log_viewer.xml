<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/bg_screen_atmosphere"
    android:padding="16dp">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="API 错误日志"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="@color/ui_title"
        android:layout_marginBottom="16dp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginBottom="16dp">

        <Button
            android:id="@+id/btn_refresh_error_logs"
            android:layout_width="0dp"
            android:layout_height="40dp"
            android:layout_weight="1"
            android:text="刷新"
            android:textSize="12sp"
            android:background="@drawable/bg_button_secondary"
            android:textColor="#fff"
            android:layout_margin="4dp" />

        <Button
            android:id="@+id/btn_copy_error_logs"
            android:layout_width="0dp"
            android:layout_height="40dp"
            android:layout_weight="1"
            android:text="复制"
            android:textSize="12sp"
            android:background="@drawable/bg_button_primary"
            android:textColor="#fff"
            android:layout_margin="4dp" />

        <Button
            android:id="@+id/btn_clear_error_logs"
            android:layout_width="0dp"
            android:layout_height="40dp"
            android:layout_weight="1"
            android:text="清空"
            android:textSize="12sp"
            android:background="@drawable/bg_button_secondary"
            android:textColor="#fff"
            android:layout_margin="4dp" />
    </LinearLayout>

    <ScrollView
        android:id="@+id/sv_error_logs"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/bg_surface_card"
        android:padding="12dp">

        <TextView
            android:id="@+id/tv_error_logs"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            android:textColor="#d32f2f"
            android:lineSpacingExtra="4dp"
            android:fontFamily="monospace" />
    </ScrollView>
</LinearLayout>
