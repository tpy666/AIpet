<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 网络权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AIpet"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- 启动页 - 应用入口 -->
        <activity
            android:name=".ui.activity.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AIpet">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 首页 -->
        <activity
            android:name=".ui.activity.MainActivity"
            android:exported="true" />

        <!-- 创建角色页 -->
        <activity
            android:name=".ui.activity.CreatePetActivity"
            android:exported="false" />

        <!-- 角色卡列表页 -->
        <activity
            android:name=".ui.activity.PetCardListActivity"
            android:exported="false" />

        <!-- 聊天页 -->
        <activity
            android:name=".ui.activity.ChatActivity"
            android:exported="false" />

        <!-- 设置页 -->
        <activity
            android:name=".ui.activity.SettingsActivity"
            android:exported="false" />

        <activity
            android:name=".ui.activity.HelpActivity"
            android:exported="false" />

        <activity
            android:name=".ui.activity.DressUpActivity"
            android:exported="false" />

        <!-- 聊天日志查看器 -->
        <activity
            android:name=".ui.activity.ChatLogViewerActivity"
            android:exported="false" />

        <!-- API 错误日志查看器 -->
        <activity
            android:name=".ui.activity.ErrorLogViewerActivity"
            android:exported="false" />

        <activity
            android:name=".ui.activity.StoreActivity"
            android:exported="false" />

        <activity
            android:name=".ui.activity.FeedActivity"
            android:exported="false" />

        <activity
            android:name=".ui.activity.OutingActivity"
            android:exported="false" />

        <activity
            android:name=".ui.activity.AffectionActivity"
            android:exported="false" />

    </application>

</manifest>